[output]
dir = "./showcase"
formats = ["png"]

[[scenes]]
type = "terminal"
command = "uv run python -c \"from lightning_kfold import KFoldTrainer; print('lightning-kfold installed successfully'); print('KFoldTrainer, KFoldDataModule, EnsembleVotingModel available')\" 2>&1"
name = "training-fold-output"
theme = "dracula"
cols = 100
maxLines = 40
